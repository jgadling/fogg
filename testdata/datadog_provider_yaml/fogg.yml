version: 2
defaults:
  backend:
    bucket: bucket
    profile: foo
    region: region
  owner: foo@example.com
  project: foo
  providers:
    datadog:
      version: "1.1.1"
  terraform_version: "1.1.1"
accounts:
  foo: {}
envs:
  bar:
    components:
      bar:
        providers:
          datadog:
            version: "2.2.2"
      bam:
        providers:
          datadog:
            enabled: false

